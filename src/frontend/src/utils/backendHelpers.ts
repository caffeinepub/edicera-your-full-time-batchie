// backendHelpers.ts is no longer used — all data operations are handled
// via dataStore.ts and useQueries.ts (localStorage-backed).
// Kept as an empty module to avoid import errors if anything still references it.

export {};
